/* require does the import of the module to our main code */
var Loopzada = require("./loop");

/* if you see what is behind Loopzada module you will see that you can create various modules inside others */
var first = 5;
var second = 20;
Loopzada(first, second);
